# Server

Backend do servidor local (Mini Server).

- `processor/` — consome MQTT e grava no InfluxDB
- `api/` — FastAPI REST + WebSocket para o dashboard

Em desenvolvimento — ver [roadmap](../docs/roadmap.md).
